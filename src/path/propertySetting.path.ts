const main = '/property-setting'

const propertySettingPath = {
    main,
    general: main + '/general',
    integration: main + '/integration',
    static: main + '/static',
}

export default propertySettingPath
