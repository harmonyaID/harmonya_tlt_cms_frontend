import { pathBasenameRoute } from '@/config/base/objectPath.config.ts'

const dashboardPath = {
    main: pathBasenameRoute('dashboard'),
    mainAlias: pathBasenameRoute('dashboard'),
}

export default dashboardPath
