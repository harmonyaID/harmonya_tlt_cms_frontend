import { ReactNode } from 'react'

export type UseDisplayModeLayoutType = {
    id: number
    name: string
    content: ReactNode
    contentIcon: ReactNode
}

export type DirectoryDisplayModelType = {
    id: number
    name: string
    content: ReactNode
    contentIcon: ReactNode
}
